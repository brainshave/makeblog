#!/usr/bin/env python

import sys

def main_layout(lines):
    a = dict()
    a["title"] = lines[0]
    # add here parsing of additional attributes in a manner of:
    # name: value
    a["body"] = lines[1:]
    return a

lines = open(sys.argv[1]).read().split()
art = main_layout(lines)
